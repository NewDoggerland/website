# FTP connection for comparing live site (source of truth) vs local WEBROOT.
# Copy to .env and fill in. .env is gitignored.
FTP_HOST=your-server.example.com
FTP_USER=your_username
FTP_PASSWORD=your_password
# Remote path on the server (e.g. / or /public_html or /httpdocs). Leave empty for root.
FTP_REMOTE_PATH=/
